[package]
name = "os"
version = "0.1.0"
edition = "2021"

[profile.release]
lto = true

[dependencies]
hal = { package = "stm32f3xx-hal", version = "0.9", features = ["rt", "stm32f302x8"] }
cortex-m-rt = "0.7"
panic-halt = "0.2"